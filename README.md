# Addressbook
This project is a C-based address book system that allows users to store, manage, and manipulate contact details. It supports operations like adding, editing, and deleting contacts, as well as searching by name or number. The data can be saved to and loaded from a file, ensuring contacts persist between program runs.
